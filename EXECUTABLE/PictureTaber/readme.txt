The program seems to fail if the path is too long. try to extract this folder into your desktop for or folder close too the root of your drive

usage:
put the pictures that follow the same naming convention as the example picture currently in the inputpictures folder 
execute pictureTaber.exe
an Output.csv file is generated you just have to import it in excel using the .csv import feature 